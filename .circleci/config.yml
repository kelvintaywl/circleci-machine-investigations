version: 2.1

jobs:
  build:
    machine: true
    steps:
      - run:
          name: Check Python
          command: python --version
      - run:
          name: Install Python packages
          command: |
            pip install futures
            pip install --upgrade awscli

workflows:
  main:
    jobs:
      - build
